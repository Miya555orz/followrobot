# Project Context

`followrobot` is a ROS 2 follow-robot project. It preserves the original FCR perception and servo stack while moving toward a TRON1 EDU wheeled-foot base.

The repository root is:

```text
/home/miya/follow_ws/src/fcr_ros2_3
```

The user fork is:

```text
https://github.com/Miya555orz/followrobot.git
```

The upstream academic/original remote is:

```text
https://github.com/cuiangA/fcr_ros2_3.git
```

Use the user fork for project handoff work unless the user explicitly asks otherwise.

This is a real robot project. Preserve user changes, inspect before modifying, and keep safety-critical work staged through simulation and review.

